<!--index.wxml-->
<view class="container">
  <view>我的故事汇</view>
  <scroll-view class="story-list" scroll-y="true" style="height: 500rpx;">
      <view wx:for="{{storyList}}" class="story-v" bindtap="goStory" id="{{item.id}}">{{item.name}}</view>
  </scroll-view>
</view>
